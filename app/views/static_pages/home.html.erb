<% if logged_in? %>
  <div class="home">
    <aside class="col-md-4">
      <section class="user_info">
        <%#= render 'shared/user_info' %>
      </section>
      <section class="bookmarks">
        <%= render 'shared/bookmark_form' %>
      </section>
    </aside>
    <div class ="bookmarks">
      <%= render 'shared/feed' %>
    </div>
  </div>
<div class="clear"></div>
<% else %>

	<% provide(:title, "Welcome") %>
  <h1>Your favorite links</h1>
  <h1>anytime, anywhere</h1>
	<p>No ads, no bs!</p>
  <br/>
  <br/>
  <h2>
  <%= link_to "Join us Today", register_path, class: 'green' %> 
  </h2>
	<p>try us<%= link_to (image_tag('baby-boy-icon.png')), '#' %> Currently in Beta stage</p>
	
  <%#= image_tag("github.png") %>
	<br/>
	<p>or  <%= link_to "Login to your account", login_path, class: 'login-link' %></p>

<% end %>
